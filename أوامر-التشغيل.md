# أوامر تشغيل سيرفر POS — مرجع سريع

مجلد المشروع:

```text
c:\Users\PIXEL\OneDrive\Desktop\pos
```

> للتفاصيل الكاملة (مشاكل شائعة، جدار ناري، إنتاج): راجع `تشغيل-السيرفر.md`

---

## 1) الإعداد (مرة واحدة لكل جهاز)

**الأسهل:** انقر مرتين على **`install-requirements.bat`** (يثبت الحزم على Python 3.13).

أو يدوياً:

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos"
& "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe" -m pip install -r requirements.txt
```

إذا رفض تفعيل البيئة:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

تحقق من Python:

```powershell
python --version
```

---

## 2) تشغيل السيرفر (يومياً)

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos"
.\.venv\Scripts\Activate.ps1
python run.py
```

بعد التشغيل افتح في المتصفح العنوان الذي يظهر في الطرفية، مثلاً:

```text
http://127.0.0.1:8011/
```

> المنفذ الافتراضي **8011**. إذا كان مشغولاً يختار السيرفر منفذاً آخر (`8020`, `8030`, `8888`…) — اقرأ سطر `POS: http://...` في الشاشة.

---

## 3) الطريقة الأسهل (نقرة واحدة)

| الملف | متى تستخدمه |
|-------|-------------|
| **`start-server.bat`** | تشغيل سريع يومي (بدون إيقاف قديم) — **الأفضل للاستخدام العادي** |
| **`restart-server.bat`** | إيقاف ثم تشغيل (عند تعارض المنفذ أو تعليق سيرفر قديم) |

من مستكشف الملفات، انقر مرتين على `start-server.bat` أو `restart-server.bat`.

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos"
.\start-server.bat
```

> إذا كان `restart-server.bat` بطيئاً في الماضي: السبب كان فحص WMI البطيء — تم استبداله بإيقاف سريع عبر المنافذ فقط.

---

## 4) منفذ محدد

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos"
.\.venv\Scripts\Activate.ps1
python run.py 8030
```

أو:

```powershell
$env:POS_PORT = "8030"
python run.py
```

---

## 5) الوصول من جهاز آخر على الشبكة (تابلت / هاتف)

`run.py` يعمل على **هذا الكمبيوتر فقط** (`127.0.0.1`). للشبكة المحلية:

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos"
.\.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --host 0.0.0.0 --port 8010
```

من الجهاز الآخر:

```text
http://عنوان-IP-الكمبيوتر:8010/
```

لمعرفة IP الكمبيوتر:

```powershell
ipconfig
```

---

## 6) إيقاف السيرفر

| الطريقة | الأمر / الإجراء |
|---------|------------------|
| من نافذة التشغيل | `Ctrl + C` |
| إيقاف يدوي | `powershell -File stop-server.ps1` |
| إيقاف + تشغيل | `.\restart-server.bat` |

---

## 7) وكيل الطباعة (طابعات الشبكة)

نافذة **PowerShell ثانية** (يُترك السيرفر يعمل في الأولى):

```powershell
cd "c:\Users\PIXEL\OneDrive\Desktop\pos\tools\print_agent"
python print_agent.py
```

تأكد أن `server_url` في `tools\print_agent\config.json` يطابق منفذ السيرفر، مثلاً:

```json
"server_url": "http://127.0.0.1:8011"
```

دليل الطابعات: `tools\print_agent\دليل-ثلاث-طابعات-وتوزيع-الطلب.md`

---

## 8) تسجيل الدخول الافتراضي

| الحقل | القيمة |
|-------|--------|
| المستخدم | `admin` |
| كلمة المرور | `admin123` |

---

## 9) ملخص الأوامر

| المطلوب | الأمر |
|---------|--------|
| تشغيل عادي | `python run.py` |
| إعادة تشغيل | `.\restart-server.bat` |
| منفذ مخصص | `python run.py 8030` |
| من الشبكة | `python -m uvicorn app.main:app --host 0.0.0.0 --port 8010` |
| وكيل الطباعة | `cd tools\print_agent` ثم `python print_agent.py` |
| إيقاف | `Ctrl+C` أو `stop-server.ps1` |

---

## 10) مشاكل شائعة (سريع)

| المشكلة | الحل |
|---------|------|
| **النافذة تتوقف عند "Starting..." ولا يظهر الرابط** | Windows يشغّل **نسخة وهمية** من Microsoft Store. استخدم **`start-server.bat`** (محدّث) أو شغّل `fix-python-path.bat` للتشخيص |
| `Activate.ps1` غير موجود | `python -m venv .venv` ثم أعد التفعيل |
| `uvicorn` غير معروف | استخدم `python run.py` أو `python -m uvicorn ...` |
| المنفذ مشغول | `.\restart-server.bat` أو `stop-server.ps1` |
| الصفحة لا تفتح | تأكد من المنفذ في سطر `POS: http://...` |
| الطباعة لا تعمل | شغّل `print_agent.py` وطابق `server_url` مع المنفذ |

### إصلاح Python البطيء (مرة واحدة — اختياري)

الإعدادات → التطبيقات → **App execution aliases** → أوقف **python.exe** و **python3.exe** (نسخ Microsoft Store).

أو استخدم المسار الحقيقي:

```powershell
& "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe" run.py
```

---

*آخر تحديث: مرجع موحّد لأوامر التشغيل اليومية.*
