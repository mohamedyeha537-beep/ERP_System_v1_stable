# تشغيل سيرفر نقطة البيع (POS)

استخدم هذا الملف في كل مرة تريد فيها تشغيل المشروع محلياً على Windows (PowerShell).

**حالتك الحالية:** إذا كان `Test-Path ".\run.py"` يعيد `True` و`Test-Path ".\.venv\Scripts\Activate.ps1"` يعيد `False` — نفّذ هذا الكتلة كاملة ثم انتقل لقسم «تشغيل السيرفر»:

```powershell
Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
python -m venv .venv
Test-Path ".\.venv\Scripts\Activate.ps1"
```

يجب أن يصبح الأخير `True`. إذا **ظلّ `False`** بعد تنفيذ `python -m venv .venv`:

1. تحقق هل وُجد أي مجلد `.venv` (حتى ناقص):

   ```powershell
   Test-Path ".\.venv"
   Get-ChildItem -Force ".\.venv" -ErrorAction SilentlyContinue
   ```

2. إن ظهرت **رسالة خطأ** عند `python -m venv .venv`، اقرأها؛ غالباً Python غير مثبت كاملاً أو صلاحيات/OneDrive تعيق الكتابة.

3. جرّب بيئة باسم **بدون نقطة** (أحياناً يساعد مع المزامنة):

   ```powershell
   python -m venv venv
   Test-Path ".\venv\Scripts\Activate.ps1"
   ```

4. أو ثبّت الحزم على **نفس `python` النظام** (بدون بيئة) ثم شغّل المشروع — للتجربة فقط:

   ```powershell
   pip install -r requirements.txt
   python run.py
   ```

ثم (عندما يصبح `Activate.ps1` موجوداً — إن أنشأت `venv` استبدل المسار بـ `.\venv\Scripts\...`):

```powershell
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python run.py
```

## المتطلبات

- **Python 3.10+** مثبت ومضاف إلى PATH (تحقق: `python --version`).

## الإعداد (مرة واحدة لكل جهاز / بعد استنساخ المشروع)

1. افتح PowerShell وانتقل لمجلد المشروع:

   ```powershell
   Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
   ```

2. (مُستحسن) **إنشاء** بيئة افتراضية ثم تفعيلها — يجب تنفيذ `python -m venv .venv` **قبل** `Activate.ps1`. إن ظهر خطأ «غير معروف» لـ `Activate.ps1` فالغالب أن مجلد `.venv` غير موجود؛ أنشئه بالأمر الأول:

   ```powershell
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
   ```

   **مهم:** أمر `python -m venv .venv` **لا يشغّل السيرفر**. هو يجهّز مجلداً داخل المشروع فقط؛ عند النجاح قد لا يظهر أي نص في الطرفية — هذا طبيعي. بعده نفّذ التفعيل ثم `pip install` ثم `python run.py` (انظر الأسفل).

   إذا ظهرت رسالة عن سياسة التنفيذ، شغّل مرة واحدة كمسؤول أو للمستخدم الحالي:

   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

3. تثبيت الحزم:

   ```powershell
   pip install -r requirements.txt
   ```

4. ملف الإعدادات `.env` (اختياري لكن مُستحسن للإنتاج):

   - انسخ `.env.example` إلى `.env` في نفس المجلد.
   - عدّل `SECRET_KEY` و`DEFAULT_ADMIN_PASSWORD` وغيرها حسب الحاجة.

   بدون `.env` يعمل التطبيق بقيم افتراضية من `infra/config.py` (مناسب للتجربة السريعة فقط).

## تشغيل السيرفر (في كل جلسة عمل)

1. انتقل للمجلد وفعّل البيئة الافتراضية إن كنت تستخدمها:

   ```powershell
   Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
   .\.venv\Scripts\Activate.ps1
   ```

2. شغّل التطبيق:

   ```powershell
   python run.py
   ```

3. افتح المتصفح على:

   **http://127.0.0.1:8010**

   - تسجيل الدخول: المستخدم الافتراضي من `.env` أو من القيم الافتراضية في الكود (`admin` / `admin123` إن لم تُعرّف كلمات مرور أخرى).

## ملاحظات

- السيرفر يعمل مع **إعادة التحميل التلقائي** (`reload=True`) عند تعديل الكود.
- لإيقاف السيرفر: في نافذة PowerShell اضغط **Ctrl+C**.
- قاعدة البيانات الافتراضية: **SQLite** ملف `pos.db` في مجلد المشروع (يُنشأ عند أول تشغيل).
- بديل لتشغيل يدوي عبر uvicorn (يفضّل مع البيئة المفعّلة):

  ```powershell
  python -m uvicorn app.main:app --host 127.0.0.1 --port 8010 --reload
  ```

  إذا كتبت `uvicorn` وحده وظهر «غير معروف»، إما لم تُفعّل `.venv` أو لم تُثبَّت الحزم؛ استخدم `python -m uvicorn` أو `python run.py`.

## عندما «يتعلّق» `python -m venv .venv` (سطر أوامر بلا نتيجة ولا يظهر `.venv`)

المشروع تحت **OneDrive** (`OneDrive\Desktop\...`) ينسخ آلاف الملفات الصغيرة عند إنشاء البيئة؛ قد يبدو الأمر **واقفاً** دقائق طويلة أو لا يُكمل المجلد. هذا شائع وليس بالضرورة أن Python معطّل.

1. **انتظر** دقيقة أو دقيقتين أول مرة؛ راقب في مدير المهام عملية `python`.
2. إذا بقي **استخدام المعالج والقرص 0%** لأكثر من دقيقتين فهذا **تعليق** وليس «نسخاً بطيئاً». أوقف العمليات: في الطرفية **Ctrl+C**، وفي مدير المهام أنهِ مهام **Python** العالقة (أو أغلق تبويب الطرفية في Cursor).
3. إن وُجد **Norton** أو حماية أخرى: أضف استثناءً لمجلد `%LOCALAPPDATA%\venvs` وللمشروع، أو عطّل الحماية **مؤقتاً** وجرب `python -m venv` مرة أخرى.
4. جرّب إنشاء بيئة **بدون pip** (أحياناً يتخطى خطوة تتعلّق):  
   `python -m venv .venv --without-pip` ثم  
   `.\.venv\Scripts\python.exe -m ensurepip --upgrade`
5. **الحل الموصى به:** أنشئ البيئة **خارج** مجلد OneDrive (مرة واحدة)، ثم اعمل من مجلد المشروع كالمعتاد:

   ```powershell
   New-Item -ItemType Directory -Force -Path "$env:LOCALAPPDATA\venvs\pos" | Out-Null
   python -m venv "$env:LOCALAPPDATA\venvs\pos"
   & "$env:LOCALAPPDATA\venvs\pos\Scripts\Activate.ps1"
   Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
   pip install -r requirements.txt
   python run.py
   ```

  في كل جلسة لاحقة: فعّل نفس المسار ثم `Set-Location` إلى المشروع ثم `python run.py`.

### تغيير البورت عند الحاجة

الآن `python run.py` يعمل على **8010** افتراضياً.

إذا أردت بورتاً مختلفاً مؤقتاً:

```powershell
python run.py 8090
```

أو:

```powershell
$env:POS_PORT = "8090"
python run.py
```

ملاحظة: في هذا الجهاز تحديداً ظهر أن `8080` و`8090` محجوبان من Windows (`WinError 10013`)، لذلك استُخدم `8010` كبورت افتراضي عامل.

6. **بديل:** من أيقونة OneDrive في شريط المهام — إيقاف مزامنة المجلد مؤقتاً أثناء إنشاء `.venv` داخل المشروع (ثم أعد المزامنة).

7. لتشخيص ما يحدث داخل المشروع:

   ```powershell
   python -m venv .venv --upgrade-deps -v
   ```

   ستظهر أسطر تقدم؛ إن توقفت عند ملف معيّن فغالباً تعارض مزامنة أو حماية.

## عندما يتعلّق `pip install` دون أي سطور (كان يعمل سابقاً)

غالباً: **حماية (مثل Norton)** تعترض الشبكة أو القرص، أو **عمليات Python/pip عالقة**، أو **شبكة/بروكسي** أمام PyPI.

1. **Ctrl+C** في الطرفية؛ في مدير المهام أنهِ **Python** و**pip** إن وُجدت.
2. ثبّت مع **تشخيص مرئي** (سترى أين يتوقف):

   ```powershell
   Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
   python -m pip install -r requirements.txt -v --no-cache-dir --default-timeout=100
   ```

3. **Norton / الحماية:** استثنِ مجلد المشروع ومجلد `pip` المؤقت (`%LOCALAPPDATA%\pip`) أو عطّل الحماية **مؤقتاً** وجرب مرة واحدة.
4. جرّب **PowerShell خارج Cursor** (من قائمة ابدأ) بنفس الأوامر — إن نجح هناك فالمشكلة مرتبطة بجلسة Cursor أو امتداداتها.
5. تأكد من **مساحة القرص** ومن أن الإنترنت يفتح `https://pypi.org` في المتصفح.
6. بعد إصلاح `pip`، إن فشل `venv` سابقاً يمكنك الاستمرار **بدون بيئة** (`python run.py`) طالما اكتمل التثبيت على نفس `python`.

## حل المشاكل الشائعة

| المشكلة | السبب | الحل |
|---------|--------|------|
| `pip install` بلا مخرجات / عالق | حماية، شبكة، عمليات عالقة | قسم **«عندما يتعلّق pip install»** أعلاه. |
| `python -m venv` لا ينهي ولا يظهر `.venv` | OneDrive/مزامنة أو بطء نسخ آلاف الملفات | قسم **«عندما يتعلّق python -m venv»** أعلاه؛ أو بيئة تحت `%LOCALAPPDATA%\venvs\pos`. |
| في مدير المهام `python` **0%** معالج وقرص طويلاً | تعليق (ليس انتظاراً طبيعياً) | أنهِ المهام، استثناء الحماية، أو `--without-pip` كما في القسم أعلاه. |
| `Activate.ps1` غير معروف | لا يوجد الملف في المسار (المجلد غير مُنشأ، أو الطرفية ليست داخل `pos`) | انظر القسم التالي **«تحقق من وجود `.venv`»**. |
| `uvicorn` غير معروف | البيئة غير مفعّلة أو الحزم غير مثبتة | فعّل `.venv` ثم `pip install -r requirements.txt`، أو شغّل `python run.py` / `python -m uvicorn ...` دون الاعتماد على أمر `uvicorn` العام. |
| بدون تفعيل سكربت التفعيل | سياسة PowerShell | استخدم مباشرة: `.\.venv\Scripts\python.exe run.py` من مجلد المشروع (يعمل حتى دون `Activate.ps1`). |

### تحقق من وجود `.venv` (عند فشل `Activate.ps1`)

1. تأكد أن المسار في الطرفية هو مجلد المشروع الذي فيه `run.py`:

   ```powershell
   Set-Location "c:\Users\PIXEL\OneDrive\Desktop\pos"
   Get-Location
   Test-Path ".\run.py"
   ```

   يجب أن يعيد `Test-Path` القيمة `True`.

2. تحقق من وجود سكربت التفعيل (المجلدات التي تبدأ بنقطة قد تحتاج `-Force`):

   ```powershell
   Test-Path ".\.venv\Scripts\Activate.ps1"
   ```

   - إذا كان **`False`**: لم تُنشأ البيئة هنا أو لم تكتمل. أعد الإنشاء:

     ```powershell
     python -m venv .venv
     ```

     انتظر حتى يعود السطر الجديد. إن استمر `False`، جرّب اسماً بدون نقطة (أحياناً يساعد مع المزامنة):

     ```powershell
     python -m venv venv
     .\venv\Scripts\Activate.ps1
     ```

3. **بدون تفعيل نهائياً** (إن وُجد `.\.venv\Scripts\python.exe`):

   ```powershell
   .\.venv\Scripts\python.exe -m pip install -r requirements.txt
   .\.venv\Scripts\python.exe run.py
   ```

   إذا استخدمت مجلد `venv` بدلاً من `.venv`، استبدل المسار بـ `.\venv\Scripts\python.exe`.

## ترحيل قاعدة البيانات (Alembic) — عند الحاجة

إذا أضفت ترحيلات جديدة في `alembic/versions/`:

```powershell
alembic upgrade head
```

---

## مزايا حديثة

### روابط لوحة التحكم
كل بطاقة (KPI) في الصفحة الرئيسية أصبحت رابطاً يفتح التقرير المفصّل المرتبط بها (مبيعات اليوم/الأمس/الشهر، الأرباح، تكلفة المباع، المصروفات، الأصول، تنبيهات النفاد...). يكفي النقر على القيمة لمعرفة من أين جاءت.

### الطاولات
- إدارة الطاولات: `/admin/tables` (يحتاج صلاحية `tables:manage`).
- في نقطة البيع: حقل اختيار الطاولة بأعلى السلة. عند تحديدها تظهر على الفاتورة وتُرسل إلى المطبخ مع تذكرة الطلب.

### الأقسام والتوجيه (KDS / واتساب / طباعة)
- اذهب إلى «إدارة الفئات» (`/catalog/categories`) واختر للقسم الجذر (مثلاً «المطعم» أو «المقهى») قناة التوجيه:
  - **بدون توجيه**: لا إجراء.
  - **شاشة المطبخ KDS**: تظهر التذكرة في `/admin/kds`.
  - **رسالة واتساب (Webhook)**: ضع في «عنوان التوجيه» رابط webhook (مثل n8n/Zapier) يستقبل JSON `{ "text": "..." }`. ينفّذ النظام `POST` إليه بمحتوى الطلب عند إتمام البيع.
  - **طباعة تذكرة منفصلة**: يظهر زر «طباعة تذكرة المطبخ» على بطاقة التذكرة في شاشة KDS.
- عند إتمام البيع تنفصل البنود تلقائياً: المطعم يحصل على بنوده، والمقهى على بنوده — والفاتورة الموحَّدة تُطبع للزبون مع تجميع البنود حسب القسم وعرض الطاولة.

### شاشة المطبخ KDS
- المسار: `/admin/kds` (يحتاج صلاحية `kds:view`).
- ألسنة (Tabs) لكل قسم له توجيه مفعّل.
- كل بطاقة تذكرة تعرض رقم البيع، الطاولة، البنود، عمر الطلب بالدقائق، وأزرار: «بدء التجهيز / جاهزة / تسليم / إعادة».
- الصفحة تتحدّث تلقائياً كل ٢٠ ثانية.

### تحليل التعادل اليومي (CVP — كم يجب أن أبيع يومياً؟)
- **المسار:** `/reports/break-even` (تقرير تفصيلي) و `/admin/recurring-costs` (تعريف التكاليف الشهرية).
- **الفكرة:** المنظومات المحاسبية الاحترافية تجمع كل تكاليفك الثابتة (رواتب، إيجار، اشتراكات، إنترنت، إهلاك أصول)، تقسمها على عدد أيام التشغيل، ثم تستخدم هامش مساهمة مبيعاتك لحساب «عتبة التعادل اليومية» — الحد الأدنى من الإيراد الذي يجب أن تحقّقه يومياً لتغطية كل تكاليفك.
- **المعادلة:** `عتبة التعادل اليومية = (التكاليف الثابتة الشهرية + إهلاك الأصول الشهري) ÷ عدد الأيام ÷ نسبة هامش المساهمة`.
- **مثالك:** 3 موظفين × 1000 + إيجار 1500 + كهرباء 600 = 5100/شهر = 170/يوم. + 200/يوم إهلاك أصول = **370 د.ل/يوم عبء ثابت**. لو هامش مساهمتك 50٪ → عتبة التعادل = 740 د.ل/يوم.
- **خطوات التشغيل:**
  1. أدخِل التزاماتك في `/admin/recurring-costs` (راتب الشيف، راتب الكاشير، إيجار، كهرباء، إنترنت، تأمين...).
  2. ارجع للوحة التحكم: ستجد بانر أزرق كبير «تحليل التعادل اليومي» يعرض العتبة وإيراد اليوم وشريط تقدم بصري.
  3. افتح `/reports/break-even` لرؤية كل يوم في الفترة: أيام رابحة (فوق العتبة) خضراء، أيام خاسرة (تحت العتبة) حمراء، مع إجمالي الفائض/العجز.
- **المرجع الأكاديمي:** Horngren, Datar, Rajan — *Cost Accounting: A Managerial Emphasis* (الفصل 3 «CVP Analysis»). تفاصيل أكثر في `/reports/accounting-help#cvp`.

### الأصول الثابتة والإهلاك (محاسبة احترافية وفق IAS 16)
- **المشكلة قبل الإصلاح:** كان النظام يخصم كامل قيمة فاتورة الأصول/الأدوات من «صافي الربح» في شهر الشراء — وهذا غير صحيح محاسبياً للأصول الثابتة (مثل الفرن أو الطابعة). فاتورة فرن بـ ٦٠٠٠ د.ل كانت تظهر «خسارة» في الشهر مع أن الفرن أصلٌ يُستخدم لسنوات.
- **الحل المعتمد:** عند إدخال فاتورة أصول (`/admin/assets/new`) أصبح لكل بند حقلان جديدان:
  - **العمر الإنتاجي بالأشهر:** `0` = مستلزم استهلاكي (يُخصم بالكامل، كأكياس وورق طابعة)؛ `> 0` = أصل ثابت يُهلَك.
  - **قيمة الخردة (Salvage):** القيمة المتوقَّعة بعد انتهاء العمر (افتراضياً 0). تُطرح من الكلفة قبل توزيع الإهلاك.
- **طريقة الحساب (القسط الثابت):** `قسط الإهلاك الشهري = (الكلفة − الخردة) ÷ العمر بالأشهر`.
  مثال: فرن ٦٠٠٠ د.ل، خردة ٦٠٠، عمر ٦٠ شهراً → ٩٠ د.ل/شهر فقط من الربح (وليس ٦٠٠٠ ضربةً واحدة).
- **التقارير الجديدة:**
  - `/reports/fixed-assets` — سجل الأصول الثابتة كاملاً: الكلفة، الإهلاك المتراكم، القيمة الدفترية، إهلاك الفترة، حالة الأصل.
  - `/reports/accounting-help` — دليل المعالجة المحاسبية مع شرح كل بند والمراجع الدولية (IAS 1, IAS 2, IAS 7, IAS 16) وروابط للمعايير الرسمية على موقع IFRS.
- **في صافي الربح:** يُطرح فقط (المستلزمات الاستهلاكية + إهلاك الفترة) بدلاً من كامل فاتورة الأصول. النتيجة: ربح يعكس الأداء التشغيلي الحقيقي.
- **في صافي التدفق النقدي:** يبقى يطرح كامل قيمة الأصول الكاملة (لأن النقد فعلاً خرج من المحفظة) — وهذا هو الفرق المتعمَّد بين الربح المحاسبي والتدفق النقدي.
- **مرجع رئيسي:** [IAS 16 — Property, Plant and Equipment](https://www.ifrs.org/issued-standards/list-of-standards/ias-16-property-plant-and-equipment/). للنسخ القديمة من فواتير الأصول التي لا تحوي عمراً افتراضياً، يُعامَل البند مستلزماً استهلاكياً (كما في السابق) لضمان عدم تعطيل البيانات الموجودة.

### النسخ الاحتياطي والاستعادة وتصفير قاعدة البيانات

الميزة موجودة على `/admin/backup` (تتطلب صلاحية `backup:manage` — مفعّلة افتراضياً للأدمن).

- **النسخ الاحتياطي**: زر «إنشاء نسخة الآن» يُنتج ملف `pos-backup-YYYYMMDD-HHMMSS.db` في مجلد `backups/` بجانب القاعدة، مستخدماً [SQLite Online Backup API](https://www.sqlite.org/backup.html) (آمن مع الكتابات المتزامنة). أو «تنزيل القاعدة الحالية» مباشرة كملف على جهازك.
- **الاستعادة**: ارفع ملف `.db` سابقاً واضغط «استعادة». النظام يتحقّق من سلامة الملف، يأخذ نسخة أمان من القاعدة الحالية تلقائياً قبل الاستبدال (تُحفظ باسم `pre-restore-...db`) ثم يستبدل الملف.
- **نقل البيانات بين الأجهزة**: نزّل الملف من الجهاز القديم → انقله بأي وسيلة (USB، Cloud، WhatsApp...) → افتح صفحة النسخ على الجهاز الجديد وارفع الملف. كل البيانات (مبيعات، مخزون، إعدادات...) تنتقل دفعةً واحدة.
- **مستويات التصفير الثلاثة (تتطلب كتابة عبارة تأكيد بالحرف):**
  1. **«محو الحركات»**: يحذف المبيعات والمشتريات والمصروفات وحركات المخزون فقط، ويُبقي كل الإعدادات والكتالوج والمستخدمين والأصول الثابتة المسجَّلة. مثالي بعد انتهاء التجربة وبدء العمل الفعلي.
  2. **«محو كل البيانات»**: يحذف الحركات + الكتالوج + الإعدادات + الفئات + التكاليف الشهرية، ويُبقي المستخدمين والصلاحيات فقط.
  3. **«تصفير المصنع الكامل»**: يحذف ملف القاعدة كلياً (بعد أرشفته في `backups/`) وينشئ قاعدة جديدة فارغة. يحتاج إعادة تشغيل الخادم لإعادة بذر `admin/admin123` والإعدادات الافتراضية.
- النظام يأخذ نسخة احتياطية تلقائية قبل أي تصفير، فلا تخسر شيئاً.

### بيانات تجريبية لشهر كامل

سكربت يولّد **شهراً كاملاً واقعياً** لتجربة النظام عملياً (مبيعات، رواتب، أصول، مصروفات...):

```bash
python scripts/seed_full_month.py            # يضيف فوق ما هو موجود
python scripts/seed_full_month.py --clear    # يصفّر الحركات أولاً ثم يبدأ نظيفاً
```

ما يولّده:
- **٣ أساليب دفع** (كاش + مصرفان).
- **١٠ تكاليف شهرية ثابتة** (٥ رواتب موظفين، إيجار، إنترنت، اشتراكات، تأمين، رخصة).
- **١٠ أصول ثابتة** بأعمار مختلفة (فرن، ثلاجة، شواية، مكيفات، طاولات، كاشير، طابعات، KDS، ديكور، أدوات مطبخ).
- **مستلزمات استهلاكية** (ورق، أكياس، تنظيف...) في ٣ فواتير شهرياً.
- **٨ فواتير شراء بضائع** (مخبز، ملحمة، خضراوات، بقالة) موزّعة على الشهر.
- **٢٠+ مصروف دوري** (كهرباء، ماء، غاز، مواصلات، صيانة، مكتبية...).
- **١٣٠+ فاتورة بيع** بتشكيلة منتجات وأساليب دفع متغيّرة، مع زيادة الازدحام في عطلة الأسبوع.

افتح بعد التشغيل:
- `/` — لوحة التحكم تعرض بيان دخل شهري كامل + تحليل التعادل اليومي.
- `/reports/break-even` — تقرير يوم بيوم: متى أنت رابح ومتى خاسر.
- `/reports/profit` — الأرباح والخسائر مع تفاصيل التكلفة.
- `/reports/comprehensive` — التقرير الشامل.
- `/reports/fixed-assets` — سجل الأصول مع الإهلاك المتراكم والقيمة الدفترية.

---

## 👥 وحدة الموارد البشرية (الموظفون والحضور والرواتب) — جديد

وحدة متخصصة موحَّدة لإدارة كل ما يخص الموظفين، تستبدل وتدمج أي إدارة سابقة كانت
موزَّعة (مثل بنود "رواتب" داخل التكاليف الشهرية).

### المكوّنات

| المسار | الوصف |
|---|---|
| `/admin/employees` | قائمة الموظفين (إضافة، تعديل، حذف، تصدير CSV) |
| `/admin/employees/new` | إضافة موظف جديد |
| `/admin/employees/{id}` | تعديل موظف |
| `/admin/attendance` | لوحة الحضور والانصراف (تسجيل سريع + سجل لكل موظف + ساعة حية) |
| `/admin/payroll` | قائمة دفعات الرواتب الشهرية |
| `/admin/payroll/{id}` | تفاصيل دفعة (بنود لكل موظف + اعتماد + دفع) |
| `/admin/advances` | إدارة سلف الموظفين (صرف/استرداد/تقرير الأدمن) |

### كيف يعمل النظام؟

1. **إضافة موظف**: تختار اسماً ووظيفة ونوع الأجر:
   - **شهري ثابت**: راتب ثابت بغض النظر عن الساعات.
   - **بالساعة**: ساعات الحضور المسجَّلة × الأجر/الساعة.
   - **باليوم**: تُحسب من الساعات ÷ ساعات اليوم المعيارية × الأجر.
   - **شهري + إضافي**: راتب أساسي + ساعات فوق المعيار × الأجر × مضاعف.

2. **الحضور والانصراف**: زرّان كبيران (دخول/خروج) مع ساعة حية. يمكن أيضاً تسجيل
   جلسات كاملة يدوياً من قبل المدير. كل جلسة لها وقت دخول وخروج وملاحظات.

3. **دفعات الرواتب الشهرية**:
   - **إنشاء**: اختر شهراً → ينشئ النظام مسودة بكل الموظفين النشطين تلقائياً مع
     حساب ساعات الحضور والإضافي، **ويُملأ حقل «سُلف» تلقائياً بمجموع السلف
     القائمة لكل موظف** (يمكنك تعديله قبل الاعتماد).
   - **تعديل**: يمكنك ضبط أي بند (مكافآت، استقطاعات، سُلف).
   - **اعتماد**: يقفل الدفعة فلا تتغيّر.
   - **دفع**: يولّد قيد مصروف موحَّد بإجمالي الرواتب يُخصم من المحفظة المختارة،
     ويظهر تلقائياً في **تقارير المصروفات والأرباح**. وتُسجَّل **استردادات
     فعلية على السلف القائمة** (FIFO الأقدم أولاً) بنفس قيمة الخصم.

4. **إدارة السلف** (`/admin/advances`):
   - **صرف سلفة**: اختر موظفاً والمبلغ والمحفظة. افتراضياً تُسجَّل كقيد مصروف
     بفئة «سلف موظفين» ليظهر التدفق النقدي. يمكن إيقاف التسجيل (ذمم فقط).
   - **استرداد يدوي**: لو دفع الموظف نقداً خارج الراتب.
   - **إلغاء**: تنازل المدير عن السلفة (تبقى في السجل بحالة CANCELLED).
   - **تقرير الأدمن قبل الرواتب**: عند فتح صفحة السلف يظهر جدول لكل موظف لديه
     سلف قائمة مع الإجمالي العام — يجب مراجعته قبل اعتماد الدفعة الشهرية.
   - **شارة في صفحة الدفعة**: بجانب كل بند يظهر «🟡 سلف قائمة: X د.ل» مع رابط
     لصفحة السلف لذلك الموظف.

### الدمج مع الأنظمة الأخرى

- **تحليل التعادل (CVP)**: مجموع رواتب الموظفين النشطين يُضمّ تلقائياً إلى
  «العبء اليومي» جنباً إلى جنب مع التكاليف الشهرية الأخرى (إيجار/كهرباء...).
- **التكاليف الشهرية الثابتة** (`/admin/recurring-costs`) أصبحت مخصَّصة لما عدا
  الرواتب (الإيجار، الكهرباء، الإنترنت، الاشتراكات، التراخيص). تنبيه واضح في
  أعلى الصفحة يُحوِّل المستخدم لوحدة الموظفين.
- **قيود الدفع**: عند دفع دفعة رواتب، يُسجَّل قيد مصروف واحد بالإجمالي، فيظهر
  في `Purchase(kind=EXPENSE, expense_category="رواتب")` ويتم إدراجه ضمن
  المصروفات الشهرية وتقرير الأرباح وتقرير المصروفات.

### الصلاحيات الجديدة (RBAC)

| الكود | الصلاحية |
|---|---|
| `hr:view` | عرض الموظفين وسجلات الحضور والرواتب |
| `hr:manage` | إدارة الموظفين واعتماد ودفع الرواتب |
| `hr:attendance` | تسجيل الحضور والانصراف |

### تصدير CSV

- `/admin/employees/export.csv` — قائمة كل الموظفين.
- `/admin/attendance/export.csv?days=30` — سجلات الحضور (لكل الموظفين أو موظف محدد).
- `/admin/payroll/{id}/export.csv` — دفعة رواتب كاملة بالتفصيل.
- `/admin/advances/export.csv` — كل السلف، أو `?only_outstanding=1` للقائمة فقط.

كلها بترميز UTF-8 BOM يفتح مباشرةً في Excel.

---

## 📊 تحسينات لوحة التحكم وتصدير التقارير (الإصدار الأخير)

### ١) الروابط السريعة في الأعلى
- جميع الروابط السريعة (نقطة البيع، التقارير، المخزون، الأصناف، الفئات، الطاولات، KDS، فواتير الشراء، المصروفات، الأصول، التكاليف الشهرية، أساليب الدفع، الوحدات، المستخدمون، الأدوار، الإعدادات، تنبيهات النفاد، النسخ الاحتياطي) أصبحت في **شريط واحد بأعلى الصفحة** مباشرةً تحت رأس الترحيب.
- يظهر الشريط بعنوان "روابط سريعة — انقر للوصول المباشر" بشكلٍ واضح.

### ٢) أقسام قابلة للطي (Collapsible)
كل قسم رئيسي في لوحة التحكم أصبح قابلاً للفتح والإغلاق بالنقر على عنوانه:
- **المبيعات** — اليوم وأمس والشهر
- **المخزون والكتالوج** — عدد الأصناف والقيمة والتنبيهات
- **تحليل التعادل اليومي** — كم يجب أن تبيع اليوم
- **بيان الدخل الشهري** — الحسبتان الرئيسيتان
- **حركة الشهر النقدية** — مدخل/خرج المحافظ (مغلق افتراضياً)

### ٣) تصدير التقارير إلى Excel/CSV
في كل صفحة تقرير يظهر شريط أزرار خضراء "📤 تصدير" يحتوي على روابط لتنزيل ملف **CSV بترميز UTF-8 BOM** يفتح مباشرةً في Microsoft Excel أو LibreOffice أو Google Sheets مع دعم كامل للعربية.

نقاط النهاية المتاحة (تتقبل `?period=day|week|month|year|custom&start=YYYY-MM-DD&end=YYYY-MM-DD`):
- `/reports/export/sales.csv` — ملخص المبيعات + توزيع طرق الدفع.
- `/reports/export/sales-detailed.csv` — كل بنود الفواتير (فاتورة × صنف × كمية × سعر).
- `/reports/export/purchases.csv` — فواتير شراء البضاعة.
- `/reports/export/expenses.csv` — المصروفات بالتصنيف.
- `/reports/export/assets.csv` — فواتير شراء الأصول/الأدوات.
- `/reports/export/profit-by-product.csv` — الربح بكل صنف (الإيراد − تكلفة).
- `/reports/export/inventory.csv` — صورة المخزون الحالية (يقبل `?only_low=1` للمنخفض فقط).
- `/reports/export/comprehensive.csv` — تقرير شامل في ملف واحد (الحسبتان + التدفق النقدي + المخزون).

> اسم الملف يحتوي تلقائياً نوع التقرير + الفترة، مثلاً:  
> `comprehensive-month-20260501-to-20260601.csv`

---

## 🌐 سياسة التشغيل الأوفلاين والمزامنة

> **القاعدة الأساسية**: المنظومة **تعمل أوفلاين بالكامل** على الجهاز المستضيف للسيرفر. الإنترنت **اختياري** ولا يؤثّر على البيع أو القيود المحاسبية إطلاقاً.

### ١) ما يعمل أوفلاين 100%

كل الميزات التالية لا تحتاج أي اتصال بالإنترنت:

| الميزة | السبب |
|---|---|
| نقطة البيع وإكمال الفواتير | محلية بالكامل (SQLite) |
| إدارة المخزون والمشتريات | محلية بالكامل |
| الحسبة المحاسبية (إهلاك، تعادل، أرباح) | محلية بالكامل |
| التقارير وتصديرها CSV | محلية بالكامل |
| الموظفون والحضور والرواتب والسلف | محلية بالكامل |
| الطباعة (A4/A5/80mm/58mm) | محلية بالكامل |
| النسخ الاحتياطي والاستعادة | محلية بالكامل |
| واجهة المستخدم (HTMX + الخطوط) | **مُحمَّلة محلياً** من `/static/vendor/` ومن خطوط النظام |

### ٢) ما يحتاج إنترنت (اختياري ولا يعطّل البيع)

| الميزة | السلوك إذا انقطع النت |
|---|---|
| تنبيه نفاد المخزون عبر **WhatsApp/Email** | يُرسل في **خيط خلفية** — البيع لا ينتظره. عند فشل الإرسال يُسجَّل الخطأ في log فقط. |
| إرسال تذاكر المطبخ عبر **WhatsApp** (KDS) | تُحفظ التذكرة بحالة `PENDING_SEND` فوراً، ثم تُحاول الإرسال في الخلفية. عند فشل الإرسال تُحوَّل إلى `ERROR` ويظل بإمكانك إعادة الإرسال يدوياً. |
| روابط معايير IFRS التعليمية | روابط `target="_blank"` فقط (تُفتح إذا ضغط المستخدم) |

**الضمانات التقنية**:
- كل المكالمات الشبكية لها `timeout=4-5 ثوانٍ` كحد أقصى.
- كل المكالمات تُلتقط استثناءاتها (`try/except`) بأمان.
- كل المكالمات تعمل في خيوط `daemon` منفصلة لها سيشن قاعدة بيانات خاصة (لا تُلوّث جلسة الطلب الحالي).
- الكود في `infra/background.py` و`modules/kds/service.py:dispatch_pending_whatsapp_tickets`.

### ٣) سيناريوهات الاستخدام المدعومة

#### السيناريو أ) جهاز واحد (الأبسط والمدعوم بالكامل)
- جهاز كاشير واحد، قاعدة بيانات SQLite محلية (`pos.db`).
- لا يحتاج لشبكة من أي نوع.
- النسخ الاحتياطي عبر `/admin/backup` يدوياً (أو سكربت دوري).

#### السيناريو ب) عدة كاشيرات في **شبكة محلية واحدة (LAN)** (مدعوم)
- جهاز خادم واحد به SQLite (مثل كمبيوتر مركزي في المحل).
- بقية الأجهزة تتصل عبر متصفح بـ `http://<عنوان-السيرفر-المحلي>:8000`.
- **لا حاجة للإنترنت** — الراوتر المحلي يكفي.
- **لا توجد مشكلة مزامنة** لأن كل الأجهزة تكتب إلى نفس الملف.
- لتحسين الأداء على شبكة بطيئة: استخدم كابل Ethernet للجهاز الخادم.

#### السيناريو ج) فروع متعددة عبر الإنترنت + مزامنة (غير مُطبَّق حالياً)
هذا يتطلب تصميماً مختلفاً (Replicated Distributed DB) ولم يُطبَّق بعد. إن احتجته:
- **الخيار 1**: استخدام **Litestream** أو **rqlite** لمزامنة SQLite تلقائياً عبر الشبكة (يحتاج إعداد).
- **الخيار 2**: ترقية الـ DB إلى **PostgreSQL** مستضافة سحابياً وكل الفروع تتصل بها (يحتاج إنترنت دائم).
- **الخيار 3**: تنفيذ **آلية مزامنة مخصَّصة** بـ UUID لكل سجل + سجل تغييرات + حل تعارض (مشروع كبير).

> **توصية**: للحالة العادية (محل أو مطعم واحد بفرع واحد) السيناريوهان أ و ب يكفيان تماماً ولا يحتاجان أي إنترنت.

### ٤) نقل البيانات بين الأجهزة (بديل المزامنة)

إذا أردت نقل البيانات بين جهازين بدون إنترنت:
1. على الجهاز المصدر: `/admin/backup` → نزّل ملف `.db`.
2. انقل الملف على فلاش/شبكة محلية.
3. على الجهاز الهدف: `/admin/backup` → استعِد الملف.

### ٥) كيف نتحقّق من الالتزام بسياسة الأوفلاين؟

شغّل اختبار الأوفلاين الجاهز:

```powershell
python scripts\_test_offline.py
```

السكربت يحاكي قطع الإنترنت تماماً (يحجب `urlopen` و`socket.create_connection` إلا لـ `localhost`)، ويتحقّق من:
- إقلاع التطبيق بدون نت.
- كل القوالب لا تطلب أي CDN.
- HTMX يُحمَّل من `/static/vendor/htmx.min.js`.
- إكمال البيع يستغرق **أقل من ثانيتين** حتى مع تفعيل تنبيهات WhatsApp/Email على عناوين معطّلة.
- لا توجد محاولات اتصال بأي خادم خارجي أثناء الطلبات الرئيسية.

نتيجة آخر تشغيل اعتُمدت: **POST /pos/checkout اكتمل في 0.04 ثانية** ولم يُسجَّل أي اتصال خارجي.

---

## 🏨 وحدة الفندق — قيد الفواتير على حساب الغرفة (دفع مؤجَّل)

مُصمَّمة للمطاعم/المقاهي **داخل فندق**: يطلب نزيل وجبة من المطعم ويُضيفها على حساب غرفته،
ثم يُسوّى الحساب لاحقاً عند تسجيل المغادرة من قبل موظف الاستقبال.

### المفاهيم الأساسية

- **غرفة فندق (`HotelRoom`)**: رقم فريد + ملاحظة داخلية اختيارية. **الغرفة كيان ثابت**
  ولا تحمل اسم نزيل دائم لأن النزلاء يتغيّرون مع الوقت.
- **قيد على غرفة (`RoomCharge`)**: فاتورة بيع *مكتملة* (المخزون مخصوم،
  المطبخ صنعها، السعر مُحتسَب) لكن **بدون تسجيل دفع**. تبقى مفتوحة حتى التسوية.
  **اسم النزيل يُحفَظ على القيد نفسه** (`guest_name_snapshot`) وليس على الغرفة،
  فالشقة الواحدة قد ينزل بها أكثر من شخص في يومٍ واحد.
- **التسوية**: موظف استقبال (مستخدم بصلاحية مختلفة عن الكاشير) يستلم النقد لاحقاً
  ويسجّل الدفع بأسلوب اختياره (كاش/مصرف).

### الصلاحيات الجديدة (RBAC)

| الكود | الوصف | لمن |
|---|---|---|
| `hotel:rooms:manage` | إدارة الغرف (إضافة/تعديل/تعطيل) | الأدمن |
| `hotel:charge` | قيد فاتورة بيع على حساب غرفة | كاشير المطعم |
| `hotel:settle` | تسوية الحسابات وتسجيل الدفع | موظف الاستقبال |

> **مهم**: الكاشير العادي لن يرى زر «على حساب غرفة فندق» إلا إذا أُضيفت له صلاحية
> `hotel:charge` صراحةً. تنفصل صلاحيات «القيد» و«التسوية» عن بعضهما لتحقيق
> فصل الواجبات (Segregation of Duties).

### كيف يعمل النظام؟

1. **إعداد الغرف** (الأدمن، مرة واحدة):
   - افتح `/admin/hotel/rooms` → أَضِف الغرف برقمها (101، 102، شقة 3، ...).
   - الحقل الوحيد المطلوب هو **رقم الغرفة**؛ يمكن إضافة ملاحظة داخلية اختيارية.
   - **لا تَكتُب اسم نزيل** هنا — النزلاء يدخلهم الكاشير يدوياً مع كل طلب.

2. **القيد عند البيع** (كاشير المطعم):
   - في `/pos` يضيف الأصناف للسلة ويضغط «إتمام البيع».
   - في صفحة الدفع يجد *تبويبين*: «💳 دفع الآن» (الافتراضي) و«🏨 على حساب غرفة فندق».
   - في تبويب الغرفة: يختار الغرفة من شبكة بطاقات، **يكتب اسم النزيل (إجباري)**
     وملاحظة (اختياري)، ثم «تأكيد البيع».
   - النتيجة: المخزون يُخصم، طلبية المطبخ تُرسَل، الفاتورة تُطبع — **بدون تحصيل**،
     واسم النزيل يُحفَظ مع هذا القيد بالذات.

3. **التسوية** (موظف الاستقبال، عند المغادرة):
   - افتح `/hotel/settle` → ترى كل الغرف ذات الرصيد المفتوح + إجمالي عام.
   - بطاقة الغرفة تُظهر **أسماء النزلاء** الفعليين عليها (قد يكون أكثر من واحد).
   - اضغط على البطاقة → ترى كل الفواتير المعلّقة بتفاصيلها واسم نزيل كل فاتورة.
   - اختر الفواتير (افتراضياً كلها) + أسلوب الدفع (كاش/مصرف) → «تسجيل الدفع».
   - النتيجة: لكل فاتورة يُنشأ `SalePayment` بالمبلغ والأسلوب،
     ويُحدَّث رصيد المحفظة في تقارير الإيرادات.

### الأثر على التقارير المحاسبية

النظام يتبع **مبدأ الاستحقاق (Accrual Accounting)**:
- **الإيرادات** (`Sale.total`) تُحتسب فور إكمال البيع → تظهر في تقارير الأرباح والمبيعات الإجمالية.
- **السيولة** (محافظ الكاش/المصارف) لا تتأثر إلا بعد التسوية → فالغرفة هي *ذمم مدينة* مؤقتة.
- بالتالي: لو لاحظت أن إجمالي الإيرادات اليومية **أكبر** من مجموع المحافظ المستلمة،
  فالفرق هو الرصيد المعلَّق على الغرف (غير مسوّى بعد).

### حماية البيانات والتحقق

- لا يمكن قيد فاتورة على غرفة إذا كانت مدفوعة بالفعل.
- لا يمكن قيد فاتورة على غرفة معطّلة.
- **اسم النزيل/العميل مطلوب** عند كل قيد (لأن الغرفة قد ينزلها أكثر من شخص).
- لا يمكن حذف غرفة عليها فواتير مفتوحة (يجب التسوية أولاً).
- لا يمكن حذف غرفة لها سجلات تسوية تاريخية (يمكن **تعطيلها** فقط — حماية للسجل المحاسبي).

### اختبار آلي للوحدة

```powershell
python scripts\_test_hotel.py        # دورة كاملة (16 فحصاً)
python scripts\_test_hotel_http.py   # واجهات HTTP والتكامل مع لوحة التحكم
```

النتائج المتوقَّعة: كل العلامات `✅` (لا توجد أخطاء أو تحذيرات).

---

## 👥 وحدة العملاء + نظام الولاء + سياقات البيع الثلاث

### الفكرة العامة

عملية البيع لها ثلاث **سياقات** (Sale Contexts) تظهر كتبويبات في POS بدل خانات منفصلة:

| السياق | الرمز في الكود | متى تستخدمه | ما يحدث وقت checkout |
|---|---|---|---|
| 🪑 **طاولة** (افتراضي) | `TABLE` | بيع داخلي للزبون على طاولة في المطعم/المقهى | دفع فوري — كاش/مصرف |
| 🏨 **شقة فندق** | `ROOM` | نزيل يطلب من المطعم ويُحسبها على غرفته | لا تحصيل الآن — يُفتح حساب على الشقة ويسوّيه الاستقبال لاحقاً |
| 📞 **طلب خارجي** | `EXTERNAL` | توصيل أو طلب خارج المحل لعميل (هاتف) | دفع فوري + **منح نقاط ولاء آلياً** للعميل |

> **ملاحظة مهمّة**: الفئات والأصناف نفسها لا تتغيّر بين السياقات؛
> ما يتغيّر هو *وجهة الفاتورة* فقط (طاولة، شقة، أو عميل).

### قاعدة العملاء (Customers)

- العميل يُعرَّف برقم **هاتفه** (مفتاح فريد). الاسم اختياري.
- تُحفظ إحصاءات تلقائياً: عدد الزيارات، إجمالي المدفوع، رصيد النقاط، آخر زيارة.
- إن كتب الكاشير رقم هاتف غير مسجَّل في تبويب «خارجي»، يُنشأ العميل تلقائياً.
- صفحة الإدارة: `/admin/customers` (صلاحية `customers:view` للعرض، `customers:manage` للتعديل/الإضافة/الحذف).
- لا يمكن **حذف** عميل عليه سجل نقاط — يُعطَّل بدلاً من ذلك (حماية للتاريخ المحاسبي).

### نظام الولاء (Loyalty)

- الإعدادات: `/admin/loyalty` — يتحكم بها الأدمن:
  - **تفعيل/تعطيل** النظام كاملاً.
  - **معدّل الكسب**: عدد النقاط لكل دينار مدفوع (افتراضي: 1 نقطة لكل دينار).
  - **قيمة النقطة بالدينار**: كم تُساوي النقطة عند الاسترداد (افتراضي: 0.1 د.ل لكل نقطة).
  - **الحد الأدنى للاستخدام**: عدد النقاط المطلوبة قبل أن يستطيع العميل صرفها.
- النقاط تُمنح **آلياً** عند إتمام الدفع لفاتورة سياقها `EXTERNAL` ومعها `customer_id`.
- يمكن للأدمن **تعديل النقاط يدوياً** (إضافة/خصم/تصحيح) من صفحة العميل، مع تسجيل ملاحظة وسجل تاريخي كامل (`loyalty_transactions`).
- منع جعل الرصيد سالباً عبر التعديل اليدوي (تحقق آمن في الخدمة).

### تبديل السياق في POS

من شريط أعلى السلة في `/pos`:
1. اضغط **🪑 طاولة** → اختر رقم الطاولة من القائمة.
2. اضغط **🏨 شقة** (تظهر فقط للكاشيرين أصحاب صلاحية `hotel:charge`) → اختر الشقة. سيُطلب اسم النزيل عند checkout.
3. اضغط **📞 خارجي** → أدخل رقم هاتف العميل (والاسم اختياري). يُنشأ آلياً إن كان جديداً.

عند دخول `/pos/checkout`:
- لو السياق `ROOM`: يُجبَر النموذج على وضع «على حساب الشقة» (لا يظهر تبويب «دفع الآن»).
- لو السياق `EXTERNAL` ومعه عميل: يظهر مؤشر أخضر بالعميل، ورصيد نقاطه الحالي، وإشعار بالنقاط القادمة.

### الصلاحيات الجديدة

| الكود | المعنى | يُمنح افتراضياً لـ |
|---|---|---|
| `customers:view` | عرض قاعدة العملاء وأرصدة النقاط | الكاشير + الأدمن |
| `customers:manage` | إنشاء/تعديل/حذف العملاء + ضبط النقاط | الأدمن |
| `loyalty:redeem` | (ميزة لاحقة) استخدام نقاط الولاء كخصم | لا أحد افتراضياً |
| `hotel:charge` | اختيار شقة في POS وقيد فاتورة عليها | الكاشير + الأدمن |

> **سكربت تنظيف**: لإزالة بيانات الاختبار التلقائية، شغّل:
>
> ```powershell
> python scripts\cleanup_test_data.py
> ```

### الأثر على المخطط

تمت إضافة:
- جدول `customers` و`loyalty_transactions` (تُنشأ آلياً عند الإقلاع).
- عمودان جديدان في `sales`:
  - `context_type` (TABLE/ROOM/EXTERNAL، افتراضي `TABLE`).
  - `customer_id` (FK → customers، nullable).

`patch_sqlite_schema` يُضيفها تلقائياً للقواعد القديمة بدون فقد بيانات.

### اختبار آلي

```powershell
python scripts\_test_customers_loyalty.py    # 52 فحصاً (يشمل HTTP)
python scripts\_test_hotel.py                # 20 فحصاً للفندق
python scripts\_test_branding.py             # 13 فحصاً للهويّة البصرية
python scripts\_smoke_test.py                # 30 صفحة (يجب أن يعطي 29/30، فقط /admin يعطي 404)
```

النتيجة المتوقَّعة: كل الاختبارات `PASS` بـ`FAIL: 0`.

---

## 🎨 الهويّة البصرية (Branding) — تخصيص الشكل

أصبح للأدمن صفحة كاملة لتخصيص الشعار والألوان من **`/admin/branding`** أو من زر «الهويّة البصرية» في:
- لوحة التحكم (`/`) — كرت وردي/بنفسجي.
- صفحة الإعدادات (`/admin/settings`) — زر علوي.

ما يمكن تخصيصه:

| العنصر | التفاصيل |
|---|---|
| 🖼️ الشعار | رفع PNG/JPG/WebP/SVG/GIF بحدّ أقصى 1MB. يظهر في الهيدر بدل الأيقونة الافتراضية. |
| 🎨 ألوان الهيدر | لونان للخلفية (gradient) + لون النص. |
| 🎨 ألوان الفوتر | خلفية + نص. |
| 🎨 الأزرار والروابط | اللون الأساسي (Primary) + لون التمييز (Accent). |
| 👁️ خيارات العرض | إخفاء الشعار أو اسم المتجر من الهيدر. |
| 🖼️ قوالب جاهزة | 7 قوالب ألوان جاهزة (داكن/أزرق/أخضر/أحمر/بنفسجي/ذهبي/سماوي) تُطبَّق بنقرة. |
| ↻ زر إعادة للافتراضي | يُعيد كل شيء وحذف الشعار. |
| 👁️ معاينة مباشرة | داخل الصفحة قبل الحفظ — كل لون يُغيَّر يظهر فوراً. |

**الأمان والصلاحيات:**
- صلاحية جديدة `branding:manage` — تأتي افتراضياً مع دور «مدير النظام».
- الكاشير وباقي الأدوار **لا يستطيعون الوصول** لصفحة الهويّة (يستلمون 403).
- التحقّق من الألوان: أي لون غير صالح يُستبدل بالافتراضي تلقائياً (دون كسر الواجهة).
- التحقّق من الشعار: امتداد + حجم — أي ملف خطر مرفوض.

**الاستعداد للمالتي-فندور:**  
كل القيم تُحفَظ في `AppSetting` (key/value)، فلاحقاً يُمكن جعل كل فندور (tenant) له صفّ خاصّ في نفس الجدول دون أي هجرة جديدة.

---

*آخر تحديث للخطوات: 2026-05-06 — يتوافق مع `run.py` والمنفذ 8000.*
