# تشغيل المنظومة على السيرفر بعد الرفع اليدوي للملفات

هذا الدليل مخصص لمن **رفع الملفات يدوياً** (WinSCP، FileZilla، نسخ مجلد USB، إلخ) **دون** الاعتماد على `git pull` من GitHub. الهدف: أن يعمل الموقع كما على `http://127.0.0.1:8010` محلياً، مع خيار فتحه من أجهزة أخرى على نفس الشبكة.

---

## 1) ما الذي يجب أن يكون على السيرفر؟

- **مجلد المشروع كاملاً** (ملفات `.py`، `app/`، `modules/`، `requirements.txt`، `run.py`، القوالب، الأصول، إلخ).
- **لا حاجة** لرفع مجلد `.venv` من جهازك (يُفضَّل إنشاؤه على السيرفر من جديد).
- **لا ترفع** إلى المجلد العام أو إلى GitHub: ملف `.env` الحساس، وملف `pos.db` إن كان يحتوي بيانات إنتاجية وتريد إبقاءه سرياً (يمكن نقل `pos.db` بأمان عبر قناة خاصة للسيرفر فقط).

إن كنت تنقل **بيانات جاهزة** من الجهاز المحلي: انسخ **`pos.db`** إلى جذر المشروع على السيرفر بجانب `run.py` (بعد أخذ نسخة احتياطية من أي ملف قديم هناك).

---

## 2) المتطلبات على السيرفر

- **Python 3.10 أو أحدث** (`python --version` أو `python3 --version`).
- **Windows Server أو Windows عادي:** استخدم PowerShell كمسارات الأوامر أدناه.
- **Linux:** استبدل تفعيل البيئة بـ `source .venv/bin/activate` واستخدم `python3` إن لزم.

---

## 3) إنشاء بيئة افتراضية وتثبيت الحزم

افتح الطرفية في **مجلد المشروع** (نفس المكان الذي فيه `run.py` و`requirements.txt`).

### Windows (PowerShell)

```powershell
cd "C:\المسار\إلى\مجلد\pos"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -U pip
pip install -r requirements.txt
```

إذا ظهرت رسالة عن **سياسة التنفيذ**:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

ثم أعد تفعيل البيئة.

### Linux (مثال)

```bash
cd /path/to/pos
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt
```

---

## 4) ملف الإعدادات `.env`

1. إن وُجد **`env.example`** أو **`.env.example`** في المشروع، انسخه إلى اسم **`.env`** في نفس جذر المشروع.
2. عدّل القيم على الأقل:
   - `SECRET_KEY` — سلسلة عشوائية طويلة.
   - `DEFAULT_ADMIN_PASSWORD` — كلمة مرور قوية للإنتاج.
   - `INTEGRATION_API_KEY` إن كنت تستخدم التكامل مع متجر خارجي.

بدون `.env` قد يعمل التطبيق بقيم افتراضية؛ ذلك **غير مناسب** للاستخدام الحقيقي على السيرفر.

---

## 5) تشغيل «مثل اللوكال هوست» (من نفس السيرفر فقط)

ملف `run.py` يشغّل التطبيق على العنوان **`127.0.0.1`** والمنفذ **`8010`** (ما لم تضبط متغير البيئة `POS_PORT` أو `PORT`، أو تمرير رقم المنفذ كوسيط).

من المجلد، مع تفعيل البيئة:

```powershell
python run.py
```

أو بمنفذ صريح:

```powershell
python run.py 8010
```

ثم من **المتصفح على نفس الجهاز** افتح:

```text
http://127.0.0.1:8010
```

هذا أقرب شيء لتجربة «كما كان على اللوكال هوست».

---

## 6) فتح الموقع من أجهزة أخرى (كاشير، طاولات، شبكة محلية)

`run.py` يربط على **`127.0.0.1`** فقط، لذلك **لن** يصل إليه الهاتف أو جهاز آخر على الشبكة. للوصول من الشبكة استخدم **uvicorn** مع استماع على جميع الواجهات:

```powershell
.\.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --host 0.0.0.0 --port 8010
```

ثم من جهاز آخر على نفس الشبكة (استبدل `IP_السيرفر` بعنوان IP الجهاز الذي يشغّل السيرفر):

```text
http://IP_السيرفر:8010
```

- افتح المنفذ **8010** (أو المنفذ الذي اخترته) في **جدار حماية Windows** أو Linux إن لزم.
- للإنتاج الطويل: يُفضَّل تشغيل دائم عبر **خدمة Windows** أو **systemd** على Linux، وليس الاعتماد على نافذة طرفية تُغلق عند الخروج.

---

## 7) ملاحظات مهمة

- **إعادة التحميل التلقائي (`reload`)** في `run.py` مناسبة للتطوير؛ على سيرفر إنتاج يُفضَّل تشغيل uvicorn **بدون** `--reload` بعد التأكد من الاستقرار.
- **قاعدة SQLite** الافتراضية: ملف **`pos.db`** في جذر المشروع (يُنشأ عند أول استخدام إن لم تنسخ ملفاً قديماً).
- **النسخ الاحتياطي:** قبل أي تحديث لاحق للملفات، انسخ **`pos.db`** و**`.env`** إلى مكان آمن.

---

## 8) عند تحديث الملفات يدوياً لاحقاً

1. أوقف تشغيل التطبيق (Ctrl+C في الطرفية أو أوقف الخدمة).
2. انسخ احتياطياً **`pos.db`**.
3. استبدل ملفات الكود من الرفع اليدوي الجديد.
4. فعّل البيئة ونفّذ: `pip install -r requirements.txt` (لالتقاط أي حزم جديدة).
5. أعد التشغيل كما في القسم 5 أو 6.

---

## 9) ربط GitHub لاحقاً (اختياري)

إن أردت لاحقاً الاستغناء عن الرفع اليدوي: أنشئ مستودعاً على GitHub، ثم على السيرفر استخدم `git clone` أو `git init` و`git remote add` و`git pull` بعد ضبط المفتاح أو التوكن. هذا لا يمنعك من التشغيل اليوم بالرفع اليدوي.

---

## ملخص سريع

| الخطوة | الأمر / الإجراء |
|--------|------------------|
| بيئة + حزم | `python -m venv .venv` ثم `pip install -r requirements.txt` |
| إعدادات | نسخ `.env.example` → `.env` وتعديل الأسرار |
| مثل localhost على السيرفر | `python run.py` ثم `http://127.0.0.1:8010` |
| من أجهزة الشبكة | `python -m uvicorn app.main:app --host 0.0.0.0 --port 8010` + فتح الجدار الناري |

إذا تعذّر التشغيل، انسخ **نص رسالة الخطأ كاملاً** من الطرفية للمراجعة.
