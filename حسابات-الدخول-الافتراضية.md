# مرجع أسماء المستخدمين وكلمات الدخول (الافتراضيات)

> **أمان:** هذه القيم منطقية للتجربة أو أول تشغيل بقاعدة فارغة. في الإنتاج عيّن كلمات مرور قوية في ملف `.env` على السيرفر، ولا ترفع `.env` إلى GitHub. احتفظ بنسخة خاصة بك من كلمات المرور الفعلية في مكان آمن (مدير كلمات مرور أو ملف خارج المستودع).

لم يعد يظهر أي تنبيه بكلمات المرور في **صفحة تسجيل الدخول**؛ راجع هذا الملف أو إعداداتك عند الحاجة.

---

## 1) مدير النظام (يُنشأ عند أول تشغيل بقاعدة بيانات فارغة)

يُقرأ من متغيرات البيئة أو من القيم الافتراضية في `infra/config.py` (انظر أيضاً `.env.example`).

| اسم المستخدم | كلمة المرور | أين تُضبط |
|--------------|-------------|-----------|
| `admin` | `admin123` | الافتراضي في الكود إن لم تُعرّف في `.env` |
| القيمة في `DEFAULT_ADMIN_USERNAME` | القيمة في `DEFAULT_ADMIN_PASSWORD` | ملف `.env` على الجهاز/السيرفر |

**مهم:** تُستخدم قيم `.env` **عند إنشاء** مستخدم الأدمن لأول مرة فقط (قاعدة جديدة). إذا كانت القاعدة موجودة مسبقاً، فكلمة مرور الأدمن هي التي سُجّلت آنذاك — وليس بالضرورة ما في `.env` الحالي.

---

## 2) مستخدمون تجريبيون (اختياري)

يُنشأون تلقائياً فقط إذا كان `SEED_DEMO_USERS=true` (الافتراضي في الكود `true`؛ يمكن تعطيله في `.env`).

كل الحسابات التالية تشترك في **نفس** كلمة المرور: القيمة في `DEMO_USERS_PASSWORD`، والافتراضي في الكود هو **`demo123`**.

| اسم المستخدم | الدور (تقريبي) |
|----------------|----------------|
| `cashier` | كاشير |
| `treasury` | أمين خزينة (مالية) |
| `support` | دعم فني (صندوق وارد واتساب) |
| `sales` | مبيعات (مراسلة عملاء) |
| `catalog` | إدخال أصناف |
| `warehouse` | مسؤول مخزن |

---

## 3) بعد تغييرك لكلمات المرور من النظام

لا يضمن هذا الملف تطابق الواقع؛ للتحقق من المستخدمين النشطين استخدم إدارة المستخدمين في لوحة الإدارة (صلاحيات كافية).
